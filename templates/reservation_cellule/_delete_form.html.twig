<form method="post" action="{{ path('app_reservation_cellule_delete', {'id': reservation_cellule.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ reservation_cellule.id) }}">
    <button class="btn">Delete</button>
</form>
