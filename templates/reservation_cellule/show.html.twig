{% extends 'base_user.html.twig' %}

{% block title %}ReservationCellule{% endblock %}

{% block body %}
    <h1>ReservationCellule</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ reservation_cellule.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ reservation_cellule.name }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ reservation_cellule.email }}</td>
            </tr>

            <tr>
                <th>Type</th>
                <td>{{ reservation_cellule.type }}</td>
            </tr>
            <tr>
                <th>Reason</th>
                <td>{{ reservation_cellule.reason }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ reservation_cellule.createdAt ? reservation_cellule.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>ReservedAt</th>
                <td>{{ reservation_cellule.reservedAt ? reservation_cellule.reservedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_reservation_cellule_index') }}">back to list</a>

    <a href="{{ path('app_reservation_cellule_edit', {'id': reservation_cellule.id}) }}">edit</a>

    {{ include('reservation_cellule/_delete_form.html.twig') }}
{% endblock %}
